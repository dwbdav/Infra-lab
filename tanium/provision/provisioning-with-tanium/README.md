# Tanium Provisioning: 7 Easy Steps Guide

Source article: https://blog.wuibaille.fr/2024/08/provisioning-with-tanium/

## Purpose

Example Windows `unattend.xml` file for a Tanium Provision deployment workflow.

## Files

- `unattend.xml` : sample Windows unattended installation file used during provisioning.

## Publication check

- No GitHub token detected.
- No API key or bearer token pattern detected.
- No email, internal URL, private IP or UNC path detected.
- The values `nomPC` and `MonOrganisation` are placeholders.
- The password value `Password1` is a lab/demo value and is intentionally kept as an example.
- The product key present in the sample is treated as a generic installation key, not a private license key.
